# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r
language: r
r_packages:
  - rsconnect
addons:
  apt:
    packages:
      - jq
      - curl
install: true
script: ./simple-rmd/deploy.sh
